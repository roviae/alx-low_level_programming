#include "main.h"

/**
 * cap_string - fxn that capitalizes all string words
 * @cap: fxn parameter
 *
 * Return: 0
 */
char *cap_string(char *cap)
{
	char *start = cap;

	while (*cap)
	{
		if (*cap >= 'A
