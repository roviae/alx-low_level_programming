#include <stdio.h>
#include <stdlib.h>
#include "main.h"

/**
 * main - program that prints it name
 * @argc: argument count
 * @argv: array of strings
 * Return: 0
 */
int main(int argc, char *argv[])
{
	argc = 0;

	printf("%s\n", argv[argc]);

	return (0);
}
