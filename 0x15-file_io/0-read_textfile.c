#include <stdio.h>
#include <unistd.h>
#include <fcntl.h>
#include <stdlib.h>
#include <string.h>

ssize_t read_textfile(const char *filename, size_t letters)
{
	char store;

	store = fopen("filename", "r");
	
	if (store)
	{
		if (store)  == NULL;
			return (0);
	}
	else
		return (0);
		
	filename = fopen("Requiescat", "r");
	dprintf(1, fgets(store, letters,filename));
