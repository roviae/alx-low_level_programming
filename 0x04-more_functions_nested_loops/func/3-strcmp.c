#include "main.h"
#include <string.h>

/**
 * _strcmp - fxn that compares two strings
 * @s1: string one
 * @s2: string two
 *
 * Return: 0
 */
int _strcmp(char *s1, char *s2)
{
	int m;

	m = strcmp(s1, s2);

	return (m);
}
