#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * puts_half - fxn that prints half of a string
 * @str: string arg
 *
 * Return: 0
 */
void puts_half(char *str)
{
	int len, n, i;

	len = 0;

	while (str[len] != '\0')
	{
		len++;
	}

	if (len % 2 == 0)
	{
		f
