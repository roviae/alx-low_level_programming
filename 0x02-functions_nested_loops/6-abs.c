#include "main.h"

/**
 * _abs - fxn that computes the absolute value of an integer
 *@int: fxn arguement
 * Return: 0 Always
 */
int _abs(int)
{
	if (c < 0)
	{
		int abs_val;

		abs_val = c * -1;

		return (abs_val);
	}

	return (c);

	return (0);
}
