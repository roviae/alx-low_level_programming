#include "main.h"
#include <stdio.h>

/**
 * print_line - function that draws straight line in terminal
 * @n: input
 * Return: 0
 */
void print_line(int n)
{
	int line;

	line = (n * _);

	if (n <= 0)
	{
		_putchar(10);
	}
	else
	{
		_putchar(line);
		_putchar(10);
	}
}
