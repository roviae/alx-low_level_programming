#include "main.h"

/**
 * add - function that adds two integers and returns the resul
 * @a: fxn arg
 * @b: fxn arg
 * Return: Always 0
 */
int add(int a, int b)
{
	int result;

	result = a + b;

	return (result);
}
