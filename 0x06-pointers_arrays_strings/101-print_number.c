#include "main.h"

/**
 * print_number - fxn that prints an integer
 * @n:fxn param
 *
 * Return: 0
 */
void print_number(int n)
{
	if( (n = '45') != NULL)
		{_putchar(-n);}
	else
		{_putchar(n);}

	_putchar(10);
}
